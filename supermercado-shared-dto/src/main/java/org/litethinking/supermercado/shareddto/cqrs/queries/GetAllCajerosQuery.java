package org.litethinking.supermercado.shareddto.cqrs.queries;

/**
 * Query DTO for retrieving all cashiers.
 * This is an immutable record class that represents a query to get all cashiers.
 */
public record GetAllCajerosQuery() {
    // Records automatically provide getters, equals, hashCode, and toString methods
}